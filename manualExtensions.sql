-- ! You have to run those commands in your database for the search to work
-- ! They have to be done manually and only once

CREATE EXTENSION IF NOT EXISTS pg_search;
CREATE EXTENSION IF NOT EXISTS vector;